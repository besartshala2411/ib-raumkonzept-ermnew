(function(root,factory){const api=factory(root||globalThis);if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.ProjectEmployeeTimeSheet=api;})(typeof window!=='undefined'?window:globalThis,function(global){'use strict';function arr(v){return Array.isArray(v)?v:[]}function txt(v){return String(v==null?'':v).trim()}function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function name(m){return txt(m&&(m.name||[m.vorname,m.nachname].filter(Boolean).join(' ')||m.displayName||m.email))||'Mitarbeiter'}function employees(project){const all=arr(global.S&&global.S.mitarbeiter),team=arr(project&&project.team).map(String);return team.length?all.filter(m=>team.includes(String(m.id))):all}function rows(days){return Array.from({length:Math.max(1,Math.min(31,Number(days)||5))},(_,i)=>({nr:i+1,datum:'',beginn:'',ende:'',pause:'',stunden:'',taetigkeit:''}))}function records(project){project.stundenzettel=arr(project.stundenzettel);return project.stundenzettel}function calcHours(row){if(!row.beginn||!row.ende)return Number(row.stunden)||0;const toMin=v=>{const p=String(v).split(':').map(Number);return(p[0]||0)*60+(p[1]||0)},mins=Math.max(0,toMin(row.ende)-toMin(row.beginn)-(Number(row.pause)||0));return Math.round(mins/60*100)/100}function total(entry){return arr(entry&&entry.zeilen).reduce((sum,r)=>sum+calcHours(r),0)}function save(project,employeeId,lines,note){const entry={id:'sz_'+Date.now().toString(36)+Math.random().toString(36).slice(2,7),mitarbeiterId:employeeId||null,projektId:project.id,datum:new Date().toISOString().slice(0,10),zeilen:lines.map(r=>({...r,stunden:calcHours(r)})),bemerkung:txt(note)};records(project).push(entry);if(typeof global.saveState==='function')global.saveState();return entry}function exportPdf(project,employee,daysOrEntry){if(!project||typeof global.pdfNewDoc!=='function'||typeof global.saveOrSharePdf!=='function'){global.toast?.('PDF-Funktion nicht verfügbar.','error');return false}const entry=daysOrEntry&&typeof daysOrEntry==='object'?daysOrEntry:null,lines=entry?arr(entry.zeilen):rows(daysOrEntry),doc=global.pdfNewDoc();let y=global.pdfHeader?global.pdfHeader(doc,'Stundenzettel'):20;const write=s=>{if(global.pdfWriteWrapped)y=global.pdfWriteWrapped(doc,String(s),15,y,180)+2;else{doc.text(String(s),15,y);y+=7}};write('Mitarbeiter: '+(employee?name(employee):'____________________________'));write('Projekt: '+txt(project.name||project.titel||'Projekt'));if(project.adresse)write('Baustelle: '+project.adresse);write('Datum       Beginn    Ende      Pause     Stunden    Tätigkeit / ausgeführte Arbeiten');lines.forEach(r=>write((r.datum||'________')+'   '+(r.beginn||'______')+'    '+(r.ende||'______')+'    '+(r.pause!==''?r.pause:'______')+'    '+(calcHours(r)||'_______')+'    '+(r.taetigkeit||'______________________________')));write('Gesamtstunden: '+(entry?total(entry).toFixed(2):'____________________'));if(entry&&entry.bemerkung)write('Bemerkungen: '+entry.bemerkung);else write('Bemerkungen: ______________________________________________________________');write('Unterschrift Mitarbeiter: ____________________   Datum: ____________________');write('Unterschrift Bauleitung: _____________________   Datum: ____________________');global.pdfFooter?.(doc);const safe=(employee?name(employee):'Mitarbeiter').replace(/[^a-z0-9_-]+/gi,'_');global.saveOrSharePdf(doc,'Stundenzettel_'+safe+'_'+txt(project.name||'Projekt').replace(/[^a-z0-9_-]+/gi,'_')+'.pdf');return true}function open(project){if(!project||typeof global.openModal!=='function')return false;const list=employees(project),lines=rows(5);global.openModal('<div class="modalHead"><div><div class="modalTitle">Stundenzettel erfassen</div><div class="pageSub">Direkt im ERM ausfüllen, speichern und anschließend als PDF drucken.</div></div><button class="iconBtn" onclick="closeModal()">×</button></div><div class="grid cols-2"><div class="formRow"><label>Mitarbeiter</label><select id="petsEmployee"><option value="">Nicht festlegen</option>'+list.map(m=>'<option value="'+esc(m.id)+'">'+esc(name(m))+'</option>').join('')+'</select></div><div class="formRow"><label>Arbeitstage</label><select id="petsDays"><option value="5">5 Tage</option><option value="7">7 Tage</option><option value="10">10 Tage</option><option value="20">20 Tage</option><option value="31">31 Tage</option></select></div></div><div id="petsRows"></div><div class="formRow"><label>Bemerkungen</label><textarea id="petsNote" placeholder="Besonderheiten, Zusatzarbeiten, Hinweise"></textarea></div><div class="modalFoot"><button class="btn" onclick="closeModal()">Abbrechen</button><button id="petsSave" class="btn primary">Speichern</button><button id="petsExport" class="btn">Speichern + PDF / Drucken</button></div>');const render=()=>{const count=Number(global.document.getElementById('petsDays')?.value||5),wrap=global.document.getElementById('petsRows');wrap.innerHTML=rows(count).map((r,i)=>'<div class="grid" style="grid-template-columns:1.1fr .8fr .8fr .7fr 2fr;gap:8px;align-items:end;margin-bottom:8px"><div class="formRow"><label>Datum</label><input data-pets="datum" data-i="'+i+'" type="date"></div><div class="formRow"><label>Beginn</label><input data-pets="beginn" data-i="'+i+'" type="time"></div><div class="formRow"><label>Ende</label><input data-pets="ende" data-i="'+i+'" type="time"></div><div class="formRow"><label>Pause Min.</label><input data-pets="pause" data-i="'+i+'" type="number" min="0" step="5"></div><div class="formRow"><label>Tätigkeit</label><input data-pets="taetigkeit" data-i="'+i+'" type="text" placeholder="z. B. Spachtelarbeiten"></div></div>').join('')};const collect=()=>{const out=rows(Number(global.document.getElementById('petsDays')?.value||5));global.document.querySelectorAll('[data-pets]').forEach(el=>{out[Number(el.dataset.i)][el.dataset.pets]=el.value});return out},doSave=withPdf=>{const mid=global.document.getElementById('petsEmployee')?.value||'',employee=arr(global.S&&global.S.mitarbeiter).find(m=>String(m.id)===String(mid))||null,entry=save(project,mid,collect(),global.document.getElementById('petsNote')?.value||'');global.toast?.('Stundenzettel gespeichert.','success');if(withPdf)exportPdf(project,employee,entry);else global.closeModal?.()};global.document.getElementById('petsDays')?.addEventListener('change',render);global.document.getElementById('petsSave')?.addEventListener('click',()=>doSave(false));global.document.getElementById('petsExport')?.addEventListener('click',()=>doSave(true));render();return true}return{name,employees,rows,records,calcHours,total,save,exportPdf,open};});