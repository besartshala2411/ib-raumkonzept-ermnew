const assert=require('assert');
let saves=0;global.saveState=()=>{saves++};global.S={mitarbeiter:[{id:'m1',vorname:'Max',nachname:'Muster'}],aufgaben:[{id:'a1',projektId:'p1',mitarbeiterId:'m1',titel:'Spachteln',status:'offen',faellig:'2026-08-30'},{id:'a2',projektId:'p1',mitarbeiterId:'m1',titel:'Fertig',status:'erledigt'}]};
const api=require('../src/modules/projects/projectSiteControl.js');const p={id:'p1',name:'Baustelle',materialbedarf:[],wochenplanung:[]};
const item=api.addMaterialNeed(p,{text:'Ardex A828',menge:'2 Sack',gemeldetVon:'Max Muster',datum:'2026-08-30'});assert(item&&item.status==='offen');assert.strictEqual(api.materialNeeds(p).length,1);assert(api.setMaterialStatus(p,item.id,'besorgt'));assert.strictEqual(item.status,'besorgt');assert.strictEqual(api.tasksForEmployee(p,global.S.mitarbeiter[0],'2026-08-30').length,1);
const week=api.weekDates('2026-08-30');assert.strictEqual(week.length,5);assert(api.setDayCrew(p,week[0],['m1']));assert.deepStrictEqual(api.planFor(p,week[0]).mitarbeiterIds,['m1']);assert(saves>=3);
const fs=require('fs'),path=require('path');const quick=fs.readFileSync(path.join(__dirname,'../src/modules/projects/projectQuickActions.js'),'utf8'),workspace=fs.readFileSync(path.join(__dirname,'../src/ui/projectWorkspace.js'),'utf8');assert(quick.includes("'Meine Baustelle heute'")&&quick.includes("'Wochenplanung'")&&quick.includes("'Material fehlt'")&&quick.includes("'Mangelzettel PDF'"));assert(workspace.includes('projectSiteControl.js'));assert(!/supabase|TaskRuntime|password|Realtime/i.test(fs.readFileSync(path.join(__dirname,'../src/modules/projects/projectSiteControl.js'),'utf8')));console.log('project site control tests passed');
