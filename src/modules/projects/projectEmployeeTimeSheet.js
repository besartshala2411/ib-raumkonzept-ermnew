(function(root,factory){const api=factory(root||globalThis);if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.ProjectEmployeeTimeSheet=api;})(typeof window!=='undefined'?window:globalThis,function(global){'use strict';function arr(v){return Array.isArray(v)?v:[]}function txt(v){return String(v==null?'':v).trim()}function name(m){return txt(m&&(m.name||[m.vorname,m.nachname].filter(Boolean).join(' ')||m.displayName||m.email))||'Mitarbeiter'}function employees(project){const all=arr(global.S&&global.S.mitarbeiter),team=arr(project&&project.team).map(String);return team.length?all.filter(m=>team.includes(String(m.id))):all}function rows(days){return Array.from({length:Math.max(1,Math.min(31,Number(days)||5))},(_,i)=>({nr:i+1,datum:'',beginn:'',ende:'',pause:'',stunden:'',taetigkeit:''}))}function exportPdf(project,employee,days){if(!project||typeof global.pdfNewDoc!=='function'||typeof global.saveOrSharePdf!=='function'){global.toast?.('PDF-Funktion nicht verfügbar.','error');return false}const doc=global.pdfNewDoc();let y=global.pdfHeader?global.pdfHeader(doc,'Stundenzettel'):20;const write=s=>{if(global.pdfWriteWrapped)y=global.pdfWriteWrapped(doc,String(s),15,y,180)+2;else{doc.text(String(s),15,y);y+=7}};write('Mitarbeiter: '+(employee?name(employee):'____________________________'));write('Projekt: '+txt(project.name||project.titel||'Projekt'));if(project.adresse)write('Baustelle: '+project.adresse);write('Zeitraum: ____________________ bis ____________________');y+=3;write('Datum       Beginn    Ende      Pause     Stunden    Tätigkeit / ausgeführte Arbeiten');rows(days).forEach(()=>write('________   ______    ______    ______    _______    ______________________________'));y+=3;write('Gesamtstunden: ____________________');write('Bemerkungen: ______________________________________________________________');write('__________________________________________________________________________');write('Unterschrift Mitarbeiter: ____________________   Datum: ____________________');write('Unterschrift Bauleitung: _____________________   Datum: ____________________');global.pdfFooter?.(doc);const safe=(employee?name(employee):'Mitarbeiter').replace(/[^a-z0-9_-]+/gi,'_');global.saveOrSharePdf(doc,'Stundenzettel_'+safe+'_'+txt(project.name||'Projekt').replace(/[^a-z0-9_-]+/gi,'_')+'.pdf');return true}function open(project){if(!project||typeof global.openModal!=='function')return false;const list=employees(project);global.openModal('<div class="modalHead"><div><div class="modalTitle">Stundenzettel erstellen</div><div class="pageSub">Leerer Stundenzettel pro Mitarbeiter zum Ausfüllen, Drucken oder als PDF.</div></div><button class="iconBtn" onclick="closeModal()">×</button></div><div class="formRow"><label>Mitarbeiter</label><select id="petsEmployee"><option value="">Nicht festlegen</option>'+list.map(m=>'<option value="'+String(m.id).replace(/"/g,'&quot;')+'">'+name(m).replace(/</g,'&lt;')+'</option>').join('')+'</select></div><div class="formRow"><label>Zeilen / Arbeitstage</label><select id="petsDays"><option value="5">5 Tage</option><option value="7">7 Tage</option><option value="10">10 Tage</option><option value="20">20 Tage</option><option value="31">Monat / 31 Zeilen</option></select></div><div class="modalFoot"><button class="btn" onclick="closeModal()">Abbrechen</button><button id="petsExport" class="btn primary">PDF / Drucken</button></div>');global.document.getElementById('petsExport')?.addEventListener('click',()=>{const id=global.document.getElementById('petsEmployee')?.value||'',employee=arr(global.S&&global.S.mitarbeiter).find(m=>String(m.id)===String(id))||null,days=Number(global.document.getElementById('petsDays')?.value||5);exportPdf(project,employee,days)});return true}return{name,employees,rows,exportPdf,open};});
