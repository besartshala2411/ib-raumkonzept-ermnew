const assert=require('assert');global.S={mitarbeiter:[{id:'m1',vorname:'Max',nachname:'Muster'},{id:'m2',name:'Erika Test'}]};const api=require('../src/modules/projects/projectEmployeeTimeSheet.js');const p={id:'p1',name:'Baustelle',team:['m1']};assert.strictEqual(api.name(global.S.mitarbeiter[0]),'Max Muster');assert.strictEqual(api.employees(p).length,1);assert.strictEqual(api.rows(5).length,5);assert.strictEqual(api.rows(99).length,31);const fs=require('fs'),path=require('path');const src=fs.readFileSync(path.join(__dirname,'../src/modules/projects/projectEmployeeTimeSheet.js'),'utf8');const shortcut=fs.readFileSync(path.join(__dirname,'../src/ui/employeeTimeSheetShortcut.js'),'utf8');const quick=fs.readFileSync(path.join(__dirname,'../src/modules/projects/projectQuickActions.js'),'utf8');const workspace=fs.readFileSync(path.join(__dirname,'../src/ui/projectWorkspace.js'),'utf8');assert(quick.includes("'Stundenzettel'"));assert(workspace.includes('projectEmployeeTimeSheet.js'));assert(workspace.includes('employeeTimeSheetShortcut.js'));assert(shortcut.includes('➕ Stundenzettel erstellen'));assert(shortcut.includes("==='stundenzettel'"));assert(shortcut.includes('Projekt auswählen'));assert(!/supabase|IB_TASKS_SUPABASE|password|passwort|Realtime/.test(src+shortcut));console.log('project employee time sheet tests passed');
